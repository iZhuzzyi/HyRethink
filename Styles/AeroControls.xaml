<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Style x:Key="AeroTabItemStyle" TargetType="{x:Type TabItem}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type TabItem}">
                    <Grid x:Name="Root" MinWidth="128" Height="50">
                        <!-- Selection Glow (kept behind content so text remains visible) -->
                        <Border x:Name="SelectionGlow"
                                Background="{StaticResource AeroTabSelectedBrush}"
                                Opacity="0"
                                IsHitTestVisible="False"/>

                        <!-- Main Background -->
                        <Border x:Name="Border"
                                Background="Transparent"
                                BorderBrush="{StaticResource AeroSeparatorBrush}"
                                BorderThickness="0,0,1,0">
                            <Grid>
                                <ContentPresenter x:Name="ContentSite"
                                                  ContentSource="Header" 
                                                  HorizontalAlignment="Center" 
                                                  VerticalAlignment="Center"
                                                  TextElement.Foreground="{StaticResource AeroTextBrush}"
                                                  TextElement.FontSize="14"
                                                  TextElement.FontWeight="SemiBold"
                                                  Margin="15,0"/>
                            </Grid>
                        </Border>
                        
                        <!-- Selection Glow/Highlight (Top Bar) -->
                        <Border x:Name="SelectionHighlight" 
                                Background="#FF3DFFF8" 
                                Height="1" 
                                VerticalAlignment="Bottom" 
                                Visibility="Collapsed"/>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter TargetName="SelectionGlow" Property="Opacity" Value="1"/>
                            <Setter TargetName="SelectionHighlight" Property="Visibility" Value="Visible"/>
                            <Setter TargetName="Border" Property="Background" Value="Transparent"/>
                            <Setter TargetName="ContentSite" Property="TextElement.Foreground" Value="#FFFFFFFF"/>
                            <Setter TargetName="ContentSite" Property="TextElement.FontWeight" Value="Bold"/>
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Border" Property="Background" Value="#1AFFFFFF"/>
                            <Setter TargetName="ContentSite" Property="TextElement.Foreground" Value="#FFFFFFFF"/>
                        </Trigger>
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsMouseOver" Value="True"/>
                                <Condition Property="IsSelected" Value="False"/>
                            </MultiTrigger.Conditions>
                            <Setter TargetName="Border" Property="Background" Value="#1AFFFFFF"/>
                        </MultiTrigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="AeroTabControlStyle" TargetType="{x:Type TabControl}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type TabControl}">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        
                        <!-- Tab Header Background (Glass Bar) -->
                        <Border Grid.Row="0" 
                                Background="Transparent"
                                BorderBrush="{StaticResource AeroBorderBrush}" 
                                BorderThickness="0,0,0,1"
                                Padding="0,0,0,0">
                            <Grid>
                                <!-- Left separator so the first tab isn't floating -->
                                <Border Width="1"
                                        HorizontalAlignment="Left"
                                        Background="{StaticResource AeroSeparatorBrush}"
                                        Opacity="0.9"
                                        IsHitTestVisible="False"/>
                                <TabPanel IsItemsHost="True" HorizontalAlignment="Center" Background="Transparent"/>
                            </Grid>
                        </Border>
                        
                        <!-- Content Area -->
                        <Border Grid.Row="1" Background="{StaticResource AeroContentBackgroundBrush}">
                            <ContentPresenter ContentSource="SelectedContent"/>
                        </Border>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>
